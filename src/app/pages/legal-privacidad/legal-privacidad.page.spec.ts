import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LegalPrivacidadPage } from './legal-privacidad.page';

describe('LegalPrivacidadPage', () => {
  let component: LegalPrivacidadPage;
  let fixture: ComponentFixture<LegalPrivacidadPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(LegalPrivacidadPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
